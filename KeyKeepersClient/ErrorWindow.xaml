<Window x:Class="KeyKeepersClient.ErrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:KeyKeepersClient"
        mc:Ignorable="d"
       Title="Error"
        SizeToContent="Height"
        Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="#1E0D0D"
            BorderBrush="#FF4C4C"
            BorderThickness="3"
            CornerRadius="15">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0"
                    Background="#2A1010"
                    CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock Text="Error"
                           FontFamily="Bahnschrift"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="White"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"/>
            </Border>

            <!-- Error text -->
            <TextBlock x:Name="ErrorMessageTextBlock"
                       Grid.Row="1"
                       TextWrapping="Wrap"
                       FontFamily="Bahnschrift"
                       FontSize="15"
                       Foreground="White"
                       Margin="10,15,10,15"/>

            <!-- Cancel Button -->
            <Button Grid.Row="2"
                    Content="Cancel"
                    Height="45"
                    Background="#2A1010"
                    Foreground="#FF6B6B"
                    BorderThickness="0"
                    FontFamily="Bahnschrift"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Cursor="Hand"
                    Margin="0,10,0,0"
                    Click="OkButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="btn"
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="10"
                                            BorderThickness="0"
                                            SnapsToDevicePixels="True">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="btn" Property="Background" Value="#3A1A1A"/>
                                            <Setter TargetName="btn" Property="Cursor" Value="Hand"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="btn" Property="Background" Value="#1E0D0D"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
